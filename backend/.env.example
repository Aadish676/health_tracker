# Paste the Neon connection string here for local PostgreSQL use.
# In Render, add the same value as the DATABASE_URL environment variable.
DATABASE_URL=postgresql://USER:PASSWORD@HOST/DATABASE?sslmode=require
